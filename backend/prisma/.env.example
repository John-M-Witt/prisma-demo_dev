# Postgres connection string in the format:
# postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/prisma_demo_dev?schema=public"

# Optional runtime flags for local development
NODE_ENV = development


# Add other local-only environment variables you rely on here:
# EXAMPLE_API_KEY=your_api_key_here
